version: "3.6"

services:
  sync:
    build:
      context: .
      args:
        JAR_FILE: rdm-sync-service/target/rdm-sync-service.jar

  sync-db:
    build:
      context: rdm-sync-db/
      args:
        DOCKER_REGISTRY: ${DOCKER_REGISTRY}
